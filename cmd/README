protoparse/cmd

This is a set of commands for reading and processing different protocols.

List of commands:
	-gobgpdump
	-recordfilegen

gobgpdump: Given a file with bgp messages, displays them in a given format (Right now, plain text)

recordfilegen: Given a type, generates a go source file for creating record files of that type
